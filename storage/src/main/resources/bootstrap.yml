spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: storage
  profiles:
    active: dev

server:
  port: 8000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka