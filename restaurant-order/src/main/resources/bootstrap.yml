spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: order
  profiles:
    active: dev

server:
  port: 8087

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka