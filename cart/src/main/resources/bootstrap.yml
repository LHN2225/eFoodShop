spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: cart
  profiles:
    active: dev

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka