spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: shipper
  profiles:
    active: dev

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://eureka-server:8761/eureka}