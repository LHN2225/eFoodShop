spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: food
  profiles:
    active: dev

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka