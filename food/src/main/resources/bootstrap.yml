spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: food
  profiles:
    active: dev

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://eureka-server:8761/eureka}