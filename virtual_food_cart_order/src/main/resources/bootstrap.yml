spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: virtual_food_cart_order
  profiles:
    active: dev

server:
  port: 9002

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka