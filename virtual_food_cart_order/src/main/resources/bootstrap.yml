spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: virtual
  profiles:
    active: dev

server:
  port: 9002

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://eureka-server:8761/eureka}